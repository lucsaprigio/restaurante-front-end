import styled from 'styled-components';

export const Container = styled.main`
  width: 100%;
  background-image: 
          linear-gradient(
           to bottom,
           #FFFFFF,
           #969cb3 
          );
`;